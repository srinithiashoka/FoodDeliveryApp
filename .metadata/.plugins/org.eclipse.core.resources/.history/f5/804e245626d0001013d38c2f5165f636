/* =======================
   FOODHUB - EQUAL HEIGHT CARDS + LOGIN BOXES
======================= */

/* RESET - NO UNDERLINES */
* { margin: 0; padding: 0; box-sizing: border-box; }
a, a:hover, a:focus, a:active, a:visited { text-decoration: none !important; color: inherit !important; }

:root {
    --primary-gradient: linear-gradient(135deg, #ff6b81 0%, #ff4757 100%);
    --navbar-gradient: linear-gradient(135deg, #ff3f4a 0%, #ff6b81 100%);
    --success-gradient: linear-gradient(135deg, #2ed573, #26c35e);
    --secondary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    --card-height: 420px;
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
    color: #333;
    line-height: 1.6;
    overflow-x: hidden;
}

.container { width: 90%; max-width: 1200px; margin: 0 auto; }

/* NAVBAR */
header {
    background: var(--navbar-gradient);
    padding: 1rem 0;
    color: white;
    position: sticky;
    top: 0;
    z-index: 1000;
    box-shadow: 0 4px 20px rgba(255, 63, 74, 0.3);
}

.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 1rem;
}

.logo {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    cursor: pointer;
}

.logo-text {
    font-size: clamp(1.5rem, 5vw, 2.2rem);
    font-weight: 900;
    background: linear-gradient(45deg, #fff, #ffeaa7);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.scooter-icon {
    font-size: clamp(1.8rem, 8vw, 2.5rem);
    background: linear-gradient(45deg, #fff, #ffeaa7);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.nav-menu {
    display: flex;
    list-style: none;
    gap: 1.5rem;
    margin: 0;
    padding: 0;
    flex-wrap: wrap;
}

.nav-menu a {
    font-weight: 600;
    color: rgba(255,255,255,0.95) !important;
    padding: 0.75rem 1.5rem;
    border-radius: 25px;
    transition: all 0.3s ease;
    display: block;
}

.nav-menu a:hover {
    color: #ffeaa7 !important;
    background: rgba(255,255,255,0.2);
    transform: translateY(-2px);
}

.nav-actions {
    display: flex;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
}

/* LOGIN/SIGNUP BUTTONS - EQUAL SIZE BOXES */
.auth-buttons {
    display: flex;
    gap: 0.75rem;
    align-items: center;
}

.btn {
    padding: 0.75rem 1.5rem;
    border: none;
    border-radius: 2rem;
    cursor: pointer;
    font-weight: 600;
    font-size: 0.95rem;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(0,0,0,0.2);
    border: none;
    flex-shrink: 0;
    min-width: 90px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    text-decoration: none !important;
}

.btn-login {
    background: rgba(255,255,255,0.2);
    color: white !important;
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255,255,255,0.3);
}

.btn-signup {
    background: var(--success-gradient);
    color: white !important;
}

.btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(0,0,0,0.3);
}

.welcome-text {
    font-weight: 500;
    color: rgba(255,255,255,0.95);
    font-size: 0.95rem;
}

.cart-icon-wrapper {
    position: relative;
    display: inline-flex;
    align-items: center;
}

.cart-icon {
    font-size: 1.5rem;
    color: white;
    cursor: pointer;
    transition: all 0.3s ease;
}

.cart-icon-wrapper:hover .cart-icon {
    transform: scale(1.2) rotate(10deg);
    color: #ffeaa7;
}

.cart-badge {
    position: absolute;
    top: -0.5rem;
    right: -0.5rem;
    background: #ff4444;
    color: #fff;
    border-radius: 50%;
    width: 1.5rem;
    height: 1.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.8rem;
    font-weight: 700;
    animation: pulse 2s infinite;
}

/* HERO & SEARCH */
.hero {
    background: var(--primary-gradient);
    color: white;
    text-align: center;
    padding: clamp(6rem, 15vw, 10rem) 1rem clamp(4rem, 8vw, 6rem);
}

.hero-text h1 {
    font-size: clamp(2rem, 6vw, 4rem);
    margin-bottom: 1rem;
    font-weight: 800;
}

.hero-text p {
    font-size: clamp(1rem, 3vw, 1.3rem);
    opacity: 0.95;
    margin-bottom: 3rem;
}

.search-box {
    background: rgba(255,255,255,0.98);
    backdrop-filter: blur(20px);
    padding: clamp(1.5rem, 4vw, 2.5rem);
    border-radius: 2.5rem;
    box-shadow: 0 25px 60px rgba(0,0,0,0.15);
    max-width: 1000px;
    margin: 0 auto;
}

.search-form {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    align-items: center;
}

@media (min-width: 481px) {
    .search-form {
        flex-direction: row;
        align-items: stretch;
        gap: 1.5rem;
    }
}

.input-wrapper {
    position: relative;
    flex: 1;
    max-width: 380px;
    width: 100%;
}

.input-wrapper input {
    width: 100%;
    padding: 1.25rem 1.5rem 1.25rem 4rem;
    border: 2px solid #e9ecef;
    border-radius: 2rem;
    font-size: 1rem;
    background: rgba(255,255,255,0.95);
    transition: all 0.3s ease;
    height: 4.25rem;
    font-family: inherit;
    box-shadow: 0 8px 25px rgba(0,0,0,0.08);
}

.input-wrapper input:focus {
    outline: none;
    border-color: #ff4757;
    box-shadow: 0 0 0 4px rgba(255,71,87,0.15);
    transform: translateY(-2px);
}

.input-wrapper i {
    position: absolute;
    left: 1.5rem;
    top: 50%;
    transform: translateY(-50%);
    color: #666;
    font-size: 1.25rem;
    z-index: 2;
}

.restaurant-input i { display: none !important; }

.btn-primary {
    background: var(--success-gradient);
    color: white !important;
    padding: 1.25rem 3rem;
    height: 4.25rem;
    font-size: 1.1rem;
}

/* EQUAL HEIGHT CARDS - CSS GRID MAGIC */
@keyframes fadeInUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
}

@keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
}

.categories, .restaurants {
    padding: clamp(5rem, 10vw, 8rem) 0;
    background: rgba(255,255,255,0.85);
}

section h2 {
    text-align: center;
    margin-bottom: 4rem;
    font-size: clamp(2rem, 5vw, 3.2rem);
    color: #ff4757;
    font-weight: 800;
}

/* CATEGORIES - PERFECT EQUAL HEIGHT */
.category-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    grid-auto-rows: var(--card-height);
    gap: 2rem;
    max-width: 1100px;
    margin: 0 auto;
}

.category-card {
    background: white;
    border-radius: 2rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: space-between;
    padding: 2rem;
    box-shadow: 0 20px 45px rgba(0,0,0,0.12);
    transition: all 0.4s ease;
    position: relative;
    overflow: hidden;
    height: 100%;
}

.category-image {
    width: 80px;
    height: 80px;
    border-radius: 20px;
    overflow: hidden;
    margin-bottom: 1rem;
    box-shadow: 0 10px 30px rgba(0,0,0,0.2);
}

.category-card img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.category-card p {
    font-weight: 700;
    font-size: 1.2rem;
    color: #333;
    margin: 0;
    text-align: center;
}

.category-card:hover {
    transform: translateY(-15px);
    box-shadow: 0 35px 70px rgba(0,0,0,0.25);
}

/* RESTAURANTS - PERFECT EQUAL HEIGHT */
.restaurant-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    grid-auto-rows: var(--card-height);
    gap: 2rem;
    max-width: 1300px;
    margin: 0 auto;
}

.restaurant-card {
    background: white;
    border-radius: 2rem;
    overflow: hidden;
    box-shadow: 0 25px 50px rgba(0,0,0,0.15);
    transition: all 0.4s ease;
    height: 100%;
    display: flex;
    flex-direction: column;
}

.restaurant-image {
    width: 100%;
    height: 200px;
    overflow: hidden;
}

.restaurant-card img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
}

.restaurant-card:hover img {
    transform: scale(1.08);
}

.restaurant-info {
    padding: 1.5rem;
    flex-grow: 1;
    display: flex;
    flex-direction: column;
}

.restaurant-info h3 {
    font-size: 1.3rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: #2c3e50;
}

.restaurant-meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1rem;
    flex-wrap: wrap;
    gap: 0.5rem;
}

.cuisine, .delivery-time {
    padding: 0.5rem 1rem;
    border-radius: 1.5rem;
    font-weight: 600;
    font-size: 0.9rem;
}

.cuisine { background: rgba(231,76,60,0.1); color: #e74c3c; }
.delivery-time { background: rgba(39,174,96,0.1); color: #27ae60; }

.address {
    color: #666;
    font-size: 0.9rem;
    margin-bottom: auto;
    line-height: 1.5;
}

.restaurant-rating {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: auto;
}

.rating { color: #f39c12; font-weight: 700; font-size: 1.1rem; }
.orders { font-size: 0.9rem; color: #95a5a6; }

.restaurant-card:hover {
    transform: translateY(-12px);
    box-shadow: 0 40px 80px rgba(0,0,0,0.25);
}

/* SEARCH RESULTS & NO RESULTS */
.search-results-info {
    background: var(--secondary-gradient);
    color: white;
    padding: 1.5rem 2.5rem;
    border-radius: 2rem;
    margin: 0 auto 3rem;
    display: flex;
    justify-content: center;
    align-items: center;
    max-width: 500px;
    font-weight: 600;
    font-size: 1.15rem;
}

.no-results {
    text-align: center;
    padding: 4rem 2rem;
    background: white;
    border-radius: 2rem;
    box-shadow: 0 25px 60px rgba(0,0,0,0.12);
    grid-column: 1 / -1;
    max-width: 500px;
    margin: 0 auto;
}

.quick-links {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-top: 2rem;
}

.quick-links a {
    background: var(--primary-gradient);
    color: white;
    padding: 1rem 2rem;
    border-radius: 2rem;
    font-weight: 600;
    box-shadow: 0 10px 25px rgba(255,71,87,0.35);
    transition: all 0.3s ease;
}

.quick-links a:hover {
    transform: translateY(-4px);
    box-shadow: 0 15px 35px rgba(255,71,87,0.45);
}

footer {
    background: linear-gradient(135deg, #2f3542 0%, #3742fa 100%);
    color: white;
    text-align: center;
    padding: 4rem 0 2.5rem;
    margin-top: 6rem;
}

/* RESPONSIVE */
@media (max-width: 480px) {
    .nav-menu { display: none; }
    .auth-buttons { flex-direction: column; gap: 0.5rem; }
    .btn-login, .btn-signup { width: 100%; max-width: 120px; }
}

@media (min-width: 769px) {
    .navbar { flex-wrap: nowrap; }
}

/* PERFORMANCE */
img { will-change: transform; loading: lazy; }
* { -webkit-tap-highlight-color: transparent; }
