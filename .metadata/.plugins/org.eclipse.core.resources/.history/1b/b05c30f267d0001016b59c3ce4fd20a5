
body {
    display: flex;
    flex-direction: column;
    min-height: 100vh;
    background: #f9f9f9;
    color: #333;
}

/* SUCCESS CARD */
.success-section {
    flex: 1;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 50px 20px;
}
.success-box {
    text-align: center;
    background: #fff;
    padding: 20px 15px;
    border-radius: 12px;
    box-shadow: 0 6px 18px rgba(0,0,0,0.1);
    max-width: 400px;
    width: 100%;
}
.success-animation {
    width: 80px;
    height: 80px;
    margin: 0 auto 15px;
    position: relative;
}
.circle {
    width: 80px;
    height: 80px;
    background: #2ed573;
    border-radius: 50%;
    animation: pop 0.5s ease-out;
}
.checkmark {
    position: absolute;
    top: 15px;
    left: 22px;
    font-size: 40px;
    color: white;
    opacity: 0;
    animation: check 0.6s ease-out 0.2s forwards;
}
@keyframes pop {0%{transform:scale(0.2);opacity:0;}100%{transform:scale(1);opacity:1;}}
@keyframes check {0%{transform:scale(0.2);opacity:0;}100%{transform:scale(1);opacity:1;}}
.success-box h1 {margin-top: 12px; color:#2ecc71; font-size:1.3rem;}
.success-text {margin-top: 8px; color:#555; font-size:0.9rem; line-height:1.4;}
.track-btn, .home-btn {
    display: inline-block;
    margin-top: 12px;
    padding: 10px 18px;
    color: white;
    font-weight: bold;
    border-radius: 6px;
    text-decoration: none;
    font-size: 0.9rem;
    transition: 0.3s;
}
.track-btn {background: #ffa500; /* Orange for track */ margin-right:10px;}
.track-btn:hover {background:#e59400; transform: scale(1.05);}
.home-btn {background: #ff4757;} /* Red for home */
.home-btn:hover {background:#e03c4c; transform: scale(1.05);}

/* FOOTER */
footer {
    background:#2f3542;
    color:white;
    text-align:center;
    padding:20px 0;
    font-size:0.85rem;
    margin-top:auto;
}
</style>