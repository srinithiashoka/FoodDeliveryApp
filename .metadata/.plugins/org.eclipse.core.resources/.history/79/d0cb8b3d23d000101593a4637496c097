/* =======================
   FOODHUB - SMALLER CARDS + BLACK HERO TEXT
======================= */

/* RESET */
* { margin: 0; padding: 0; box-sizing: border-box; }
a, a:hover, a:focus, a:active, a:visited { text-decoration: none !important; color: inherit !important; }

:root {
    --primary-gradient: linear-gradient(135deg, #ff6b81 0%, #ff4757 100%);
    --navbar-gradient: linear-gradient(135deg, #ff3f4a 0%, #ff6b81 100%);
    --success-gradient: linear-gradient(135deg, #2ed573, #26c35e);
    --secondary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    --card-height: 280px; /* REDUCED FROM 420px */
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
    color: #333;
    line-height: 1.6;
    overflow-x: hidden;
}

.container { width: 90%; max-width: 1200px; margin: 0 auto; }

/* NAVBAR - SAME */
header {
    background: var(--navbar-gradient);
    padding: 1rem 0;
    color: white;
    position: sticky;
    top: 0;
    z-index: 1000;
    box-shadow: 0 4px 20px rgba(255, 63, 74, 0.3);
}

.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 1rem;
}

.logo {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    cursor: pointer;
}

.logo-text {
    font-size: clamp(1.5rem, 5vw, 2.2rem);
    font-weight: 900;
    background: linear-gradient(45deg, #fff, #ffeaa7);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.scooter-icon {
    font-size: clamp(1.8rem, 8vw, 2.5rem);
    background: linear-gradient(45deg, #fff, #ffeaa7);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.nav-menu {
    display: flex;
    list-style: none;
    gap: 1.5rem;
    margin: 0;
    padding: 0;
    flex-wrap: wrap;
}

.nav-menu a {
    font-weight: 600;
    color: rgba(255,255,255,0.95) !important;
    padding: 0.75rem 1.5rem;
    border-radius: 25px;
    transition: all 0.3s ease;
    display: block;
}

.nav-menu a:hover {
    color: #ffeaa7 !important;
    background: rgba(255,255,255,0.2);
    transform: translateY(-2px);
}

.nav-actions { display: flex; align-items: center; gap: 1rem; flex-wrap: wrap; }

.auth-buttons {
    display: flex;
    gap: 0.75rem;
    align-items: center;
}

.btn {
    padding: 0.75rem 1.5rem;
    border: none;
    border-radius: 2rem;
    cursor: pointer;
    font-weight: 600;
    font-size: 0.95rem;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(0,0,0,0.2);
    border: none;
    flex-shrink: 0;
    min-width: 90px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
}

.btn-login {
    background: rgba(255,255,255,0.2);
    color: white !important;
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255,255,255,0.3);
}

.btn-signup {
    background: var(--success-gradient);
    color: white !important;
}

.btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(0,0,0,0.3);
}

.welcome-text {
    font-weight: 500;
    color: rgba(255,255,255,0.95);
    font-size: 0.95rem;
}

.cart-icon-wrapper {
    position: relative;
    display: inline-flex;
    align-items: center;
}

.cart-icon {
    font-size: 1.5rem;
    color: white;
    cursor: pointer;
    transition: all 0.3s ease;
}

.cart-icon-wrapper:hover .cart-icon {
    transform: scale(1.2) rotate(10deg);
    color: #ffeaa7;
}

.cart-badge {
    position: absolute;
    top: -0.5rem;
    right: -0.5rem;
    background: #ff4444;
    color: #fff;
    border-radius: 50%;
    width: 1.5rem;
    height: 1.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.8rem;
    font-weight: 700;
    animation: pulse 2s infinite;
}

/* HERO SECTION - REDUCED HEIGHT + BLACK TEXT */
.hero {
    background: var(--primary-gradient);
    color: #000000 !important; /* SINGLE BLACK COLOR */
    text-align: center;
    padding: clamp(4rem, 10vw, 6rem) 1rem clamp(2rem, 6vw, 4rem); /* REDUCED PADDING */
}

.hero-text h1 {
    font-size: clamp(1.8rem, 5vw, 3rem); /* REDUCED SIZE */
    margin-bottom: 0.75rem;
    font-weight: 800;
    color: #000000 !important; /* BLACK */
    text-shadow: none;
}

.hero-text p {
    font-size: clamp(0.95rem, 2.5vw, 1.1rem); /* REDUCED SIZE */
    opacity: 0.9;
    margin-bottom: 2rem; /* REDUCED */
    color: #000000 !important; /* BLACK */
}

.search-box {
    background: rgba(255,255,255,0.98);
    backdrop-filter: blur(20px);
    padding: clamp(1.5rem, 4vw, 2rem); /* REDUCED */
    border-radius: 2rem; /* REDUCED */
    box-shadow: 0 20px 50px rgba(0,0,0,0.15); /* REDUCED */
    max-width: 900px; /* REDUCED */
    margin: 0 auto;
}

.search-form {
    display: flex;
    flex-direction: column;
    gap: 1rem; /* REDUCED */
    align-items: center;
}

@media (min-width: 481px) {
    .search-form {
        flex-direction: row;
        align-items: stretch;
        gap: 1.25rem; /* REDUCED */
    }
}

.input-wrapper {
    position: relative;
    flex: 1;
    max-width: 340px; /* REDUCED */
    width: 100%;
}

.input-wrapper input {
    width: 100%;
    padding: 1rem 1.25rem 1rem 3.5rem; /* REDUCED */
    border: 2px solid #e9ecef;
    border-radius: 1.75rem; /* REDUCED */
    font-size: 0.95rem; /* REDUCED */
    background: rgba(255,255,255,0.95);
    transition: all 0.3s ease;
    height: 3.75rem; /* REDUCED */
    font-family: inherit;
    box-shadow: 0 6px 20px rgba(0,0,0,0.08); /* REDUCED */
}

.input-wrapper input:focus {
    outline: none;
    border-color: #ff4757;
    box-shadow: 0 0 0 3px rgba(255,71,87,0.15);
    transform: translateY(-1px);
}

.input-wrapper i {
    position: absolute;
    left: 1.25rem;
    top: 50%;
    transform: translateY(-50%);
    color: #666;
    font-size: 1.1rem; /* REDUCED */
    z-index: 2;
}

.restaurant-input i { display: none !important; }

.btn-primary {
    background: var(--success-gradient);
    color: white !important;
    padding: 1rem 2.5rem; /* REDUCED */
    height: 3.75rem; /* REDUCED */
    font-size: 1rem; /* REDUCED */
}

/* CATEGORIES - SMALLER CARDS */
.categories, .restaurants {
    padding: clamp(3rem, 8vw, 5rem) 0; /* REDUCED */
    background: rgba(255,255,255,0.85);
}

section h2 {
    text-align: center;
    margin-bottom: 3rem; /* REDUCED */
    font-size: clamp(1.8rem, 4vw, 2.8rem); /* REDUCED */
    color: #ff4757;
    font-weight: 800;
}

/* SMALLER CATEGORY CARDS */
.category-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr)); /* REDUCED 220px → 160px */
    grid-auto-rows: var(--card-height); /* 280px */
    gap: 1.5rem; /* REDUCED */
    max-width: 1000px; /* REDUCED */
    margin: 0 auto;
}

.category-card {
    background: white;
    border-radius: 1.5rem; /* REDUCED */
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: space-between;
    padding: 1.25rem 1rem; /* REDUCED */
    box-shadow: 0 15px 35px rgba(0,0,0,0.12); /* REDUCED */
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
    height: 100%;
}

.category-image {
    width: 60px; /* REDUCED 80px */
    height: 60px; /* REDUCED */
    border-radius: 15px; /* REDUCED */
    overflow: hidden;
    margin-bottom: 0.75rem; /* REDUCED */
    box-shadow: 0 8px 25px rgba(0,0,0,0.2);
}

.category-card img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.category-card p {
    font-weight: 700;
    font-size: 1rem; /* REDUCED 1.2rem */
    color: #333;
    margin: 0;
    text-align: center;
}

.category-card:hover {
    transform: translateY(-10px); /* REDUCED */
    box-shadow: 0 25px 50px rgba(0,0,0,0.22); /* REDUCED */
}

/* RESTAURANT CARDS - SMALLER */
.restaurant-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(260px, 1fr)); /* REDUCED 300px */
    grid-auto-rows: var(--card-height); /* 280px */
    gap: 1.75rem; /* REDUCED */
    max-width: 1200px;
    margin: 0 auto;
}

.restaurant-card {
    background: white;
    border-radius: 1.75rem; /* REDUCED */
    overflow: hidden;
    box-shadow: 0 20px 40px rgba(0,0,0,0.15); /* REDUCED */
    transition: all 0.3s ease;
    height: 100%;
    display: flex;
    flex-direction: column;
}

.restaurant-image {
    width: 100%;
    height: 160px; /* REDUCED 200px */
    overflow: hidden;
}

.restaurant-card img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.4s ease;
}

.restaurant-card:hover img {
    transform: scale(1.06);
}

.restaurant-info {
    padding: 1.25rem; /* REDUCED */
    flex-grow: 1;
    display: flex;
    flex-direction: column;
}

.restaurant-info h3 {
    font-size: 1.2rem; /* REDUCED */
    font-weight: 700;
    margin-bottom: 0.75rem; /* REDUCED */
    color: #2c3e50;
}

.restaurant-meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.75rem; /* REDUCED */
    flex-wrap: wrap;
    gap: 0.5rem;
}

.cuisine, .delivery-time {
    padding: 0.4rem 0.8rem; /* REDUCED */
    border-radius: 1.25rem; /* REDUCED */
    font-weight: 600;
    font-size: 0.85rem; /* REDUCED */
}

.address {
    color: #666;
    font-size: 0.85rem; /* REDUCED */
    margin-bottom: auto;
    line-height: 1.4;
}

.restaurant-rating {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: auto;
}

.rating { color: #f39c12; font-weight: 700; font-size: 1rem; }
.orders { font-size: 0.85rem; color: #95a5a6; }

.restaurant-card:hover {
    transform: translateY(-8px); /* REDUCED */
    box-shadow: 0 30px 60px rgba(0,0,0,0.22); /* REDUCED */
}

/* OTHER SECTIONS - SAME */
.search-results-info {
    background: var(--secondary-gradient);
    color: white;
    padding: 1.25rem 2rem; /* REDUCED */
    border-radius: 1.75rem;
    margin: 0 auto 2.5rem; /* REDUCED */
    display: flex;
    justify-content: center;
    align-items: center;
    max-width: 450px; /* REDUCED */
    font-weight: 600;
    font-size: 1.1rem;
}

@keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
}

footer {
    background: linear-gradient(135deg, #2f3542 0%, #3742fa 100%);
    color: white;
    text-align: center;
    padding: 3rem 0 2rem; /* REDUCED */
    margin-top: 5rem; /* REDUCED */
}

/* RESPONSIVE */
@media (max-width: 480px) {
    .nav-menu { display: none; }
    .auth-buttons { flex-direction: column; gap: 0.5rem; }
    .category-grid { grid-template-columns: repeat(auto-fit, minmax(140px, 1fr)); }
    .restaurant-grid { grid-template-columns: 1fr; }
}

@media (min-width: 769px) {
    .navbar { flex-wrap: nowrap; }
}
