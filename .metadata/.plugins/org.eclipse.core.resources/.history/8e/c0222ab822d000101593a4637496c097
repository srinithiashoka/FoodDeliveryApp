/* =======================
   FOODHUB - COMPLETE PRODUCTION CSS (NO UNDERLINES)
======================= */

/* =======================
   RESET - ZERO UNDERLINES EVERYWHERE
======================= */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

a, a:hover, a:focus, a:active, a:visited {
    text-decoration: none !important;
    color: inherit !important;
}

:root {
    --primary-gradient: linear-gradient(135deg, #ff6b81 0%, #ff4757 100%);
    --navbar-gradient: linear-gradient(135deg, #ff3f4a 0%, #ff6b81 100%);
    --success-gradient: linear-gradient(135deg, #2ed573, #26c35e);
    --secondary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
    color: #333;
    line-height: 1.6;
    overflow-x: hidden;
}

.container {
    width: 90%;
    max-width: 1200px;
    margin: 0 auto;
}

/* =======================
   NAVBAR - PERFECT CLEAN
======================= */
header {
    background: var(--navbar-gradient);
    padding: 1rem 0;
    color: white;
    position: sticky;
    top: 0;
    z-index: 1000;
    box-shadow: 0 4px 20px rgba(255, 63, 74, 0.3);
    backdrop-filter: blur(10px);
}

.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 1rem;
}

.logo {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    cursor: pointer;
}

.logo-text {
    font-size: clamp(1.5rem, 5vw, 2.2rem);
    font-weight: 900;
    background: linear-gradient(45deg, #fff, #ffeaa7);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.scooter-icon {
    font-size: clamp(1.8rem, 8vw, 2.5rem);
    background: linear-gradient(45deg, #fff, #ffeaa7);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.nav-menu {
    display: flex;
    list-style: none;
    gap: 1.5rem;
    margin: 0;
    padding: 0;
    flex-wrap: wrap;
}

.nav-menu li {
    list-style: none;
}

.nav-menu a {
    font-weight: 600;
    color: rgba(255,255,255,0.95) !important;
    padding: 0.75rem 1.5rem;
    border-radius: 25px;
    transition: all 0.3s ease;
    display: block;
    position: relative;
    overflow: hidden;
    text-decoration: none !important;
}

.nav-menu a::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
    transition: left 0.5s;
}

.nav-menu a:hover::before {
    left: 100%;
}

.nav-menu a:hover {
    color: #ffeaa7 !important;
    background: rgba(255,255,255,0.2);
    transform: translateY(-2px);
}

.nav-actions {
    display: flex;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
}

.welcome-text {
    font-weight: 500;
    color: rgba(255,255,255,0.95);
    font-size: 0.95rem;
}

.cart-icon-wrapper {
    position: relative;
    display: inline-flex;
    align-items: center;
}

.cart-icon {
    font-size: 1.5rem;
    color: white;
    cursor: pointer;
    transition: all 0.3s ease;
    text-decoration: none !important;
}

.cart-icon-wrapper:hover .cart-icon {
    transform: scale(1.2) rotate(10deg);
    color: #ffeaa7;
}

.cart-badge {
    position: absolute;
    top: -0.5rem;
    right: -0.5rem;
    background: #ff4444 !important;
    color: #fff !important;
    border-radius: 50%;
    width: 1.5rem;
    height: 1.5rem;
    display: flex !important;
    align-items: center;
    justify-content: center;
    font-size: 0.8rem;
    font-weight: 700;
    animation: pulse 2s infinite;
}

/* =======================
   BUTTONS
======================= */
.btn {
    padding: 1.25rem 3rem;
    border: none;
    border-radius: 2rem;
    cursor: pointer;
    font-weight: 600;
    font-size: 1.1rem;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    background: var(--success-gradient);
    color: white !important;
    height: 4.25rem;
    flex-shrink: 0;
    box-shadow: 0 8px 25px rgba(46,213,115,0.4);
    display: flex;
    align-items: center;
    gap: 0.5rem;
    text-decoration: none !important;
}

.btn-login, .btn-signup {
    padding: 0.75rem 1.5rem;
    height: auto;
    font-size: 0.95rem;
}

.btn:hover {
    transform: translateY(-3px) scale(1.05);
    box-shadow: 0 12px 35px rgba(46,213,115,0.5);
}

/* =======================
   HERO SECTION
======================= */
.hero {
    background: var(--primary-gradient);
    color: white;
    text-align: center;
    padding: clamp(6rem, 15vw, 10rem) 1rem clamp(4rem, 8vw, 6rem);
}

.hero-content {
    animation: fadeInUp 1s ease-out;
}

.hero-text h1 {
    font-size: clamp(2rem, 6vw, 4rem);
    margin-bottom: 1rem;
    font-weight: 800;
    letter-spacing: -0.02em;
}

.hero-text p {
    font-size: clamp(1rem, 3vw, 1.3rem);
    opacity: 0.95;
    margin-bottom: 3rem;
}

/* =======================
   SEARCH BOX - STRAIGHT LINE PERFECT
======================= */
.search-box {
    background: rgba(255,255,255,0.98);
    backdrop-filter: blur(20px);
    padding: clamp(1.5rem, 4vw, 2.5rem);
    border-radius: 2.5rem;
    box-shadow: 0 25px 60px rgba(0,0,0,0.15);
    border: 1px solid rgba(255,255,255,0.2);
    max-width: 1000px;
    margin: 0 auto;
    animation: fadeInUp 1s ease-out 0.4s both;
}

.search-form {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    align-items: center;
}

.input-wrapper {
    position: relative;
    flex: 1;
    max-width: 380px;
    width: 100%;
}

.input-wrapper input {
    width: 100%;
    padding: 1.25rem 1.5rem 1.25rem 4rem;
    border: 2px solid #e9ecef;
    border-radius: 2rem;
    font-size: 1rem;
    background: rgba(255,255,255,0.95);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    height: 4.25rem;
    font-family: inherit;
    box-shadow: 0 8px 25px rgba(0,0,0,0.08);
}

.input-wrapper input:focus {
    outline: none;
    border-color: #ff4757;
    box-shadow: 0 0 0 4px rgba(255,71,87,0.15), 0 12px 30px rgba(0,0,0,0.12);
    transform: translateY(-2px);
}

.input-wrapper i {
    position: absolute;
    left: 1.5rem;
    top: 50%;
    transform: translateY(-50%);
    color: #666;
    font-size: 1.25rem;
    z-index: 2;
    transition: all 0.3s ease;
}

.input-wrapper input:focus + i {
    color: #ff4757;
    transform: translateY(-50%) scale(1.1);
}

/* HIDE ICON FROM RESTAURANT INPUT */
.restaurant-input i {
    display: none !important;
}

/* =======================
   RESPONSIVE - STRAIGHT LINE ON DESKTOP
======================= */
@media (min-width: 481px) {
    .search-form {
        flex-direction: row;
        align-items: stretch;
        gap: 1.5rem;
    }
    
    .input-wrapper {
        max-width: none;
    }
}

@media (max-width: 480px) {
    .nav-menu {
        display: none;
    }
    
    .nav-actions {
        gap: 0.5rem;
    }
}

/* =======================
   CATEGORIES & RESTAURANTS (UNCHANGED - WORKING PERFECT)
======================= */
@keyframes fadeInUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
}

@keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
}

.categories, .restaurants {
    padding: clamp(5rem, 10vw, 8rem) 0;
    background: rgba(255,255,255,0.85);
}

section h2 {
    text-align: center;
    margin-bottom: 4rem;
    font-size: clamp(2rem, 5vw, 3.2rem);
    color: #ff4757;
    font-weight: 800;
}

.category-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(14rem, 1fr));
    gap: 2.5rem;
    max-width: 1100px;
    margin: 0 auto;
}

.category-card {
    background: white;
    border-radius: 2.5rem;
    height: 16rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-end;
    padding: 2.5rem 2rem 2rem;
    box-shadow: 0 20px 45px rgba(0,0,0,0.12);
    transition: all 0.4s ease;
    position: relative;
    overflow: hidden;
    text-decoration: none !important;
}

.category-card:hover {
    transform: translateY(-1.5rem);
    box-shadow: 0 35px 70px rgba(0,0,0,0.25);
}

.restaurant-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(22rem, 1fr));
    gap: 2.5rem;
    max-width: 1300px;
    margin: 0 auto;
}

.restaurant-card {
    background: white;
    border-radius: 2.5rem;
    overflow: hidden;
    box-shadow: 0 25px 50px rgba(0,0,0,0.15);
    transition: all 0.4s ease;
    text-decoration: none !important;
}

.restaurant-card:hover {
    transform: translateY(-1.25rem);
    box-shadow: 0 40px 80px rgba(0,0,0,0.25);
}

.search-results-info {
    background: var(--secondary-gradient);
    color: white;
    padding: 1.5rem 2.5rem;
    border-radius: 2rem;
    margin: 0 auto 3rem;
    display: flex;
    justify-content: center;
    align-items: center;
    max-width: 500px;
    font-weight: 600;
    font-size: 1.15rem;
}

footer {
    background: linear-gradient(135deg, #2f3542 0%, #3742fa 100%);
    color: white;
    text-align: center;
    padding: 4rem 0 2.5rem;
    margin-top: 8rem;
}

/* Scrollbar */
::-webkit-scrollbar {
    width: 10px;
}

::-webkit-scrollbar-thumb {
    background: var(--primary-gradient);
    border-radius: 10px;
}
