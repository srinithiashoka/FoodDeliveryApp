/* =======================
   RESET
======================= */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
}

a {
    text-decoration: none;
    color: inherit;
}

/* =======================
   HEADER
======================= */
header {
    width: 100%;
    background: #ff3b4e; /* darker red for header */
    padding: 18px 0;
    position: sticky;
    top: 0;
    z-index: 1000;
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
}

.navbar {
    width: 90%;
    max-width: 1250px;
    margin: auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
}

.logo {
    font-size: 32px;
    font-weight: 700;
    color: #fff;
    letter-spacing: 1px;
}

/* Nav Links */
.navbar ul {
    list-style: none;
    display: flex;
    gap: 35px;
}

.navbar ul li a {
    color: #fff;
    font-size: 16px;
    font-weight: 500;
    padding: 8px 12px;
    transition: 0.3s;
}

.navbar ul li a:hover {
    background: rgba(255, 255, 255, 0.2);
    border-radius: 8px;
}

/* Login & SignUp Buttons */
.login-btn, .signup-btn {
    padding: 10px 22px;
    border-radius: 12px; /* more square but soft corners */
    border: none;
    cursor: pointer;
    font-weight: 600;
    transition: 0.3s;
}

.login-btn {
    background: #ff7d8a;
    color: white;
}

.login-btn:hover {
    background: #ff5b6a;
}

.signup-btn {
    background: #c70039;
    color: white;
}

.signup-btn:hover {
    background: #900028;
}

/* =======================
   HERO SECTION (Gradient Background)
======================= */
.hero {
    padding: 120px 20px 90px 20px;
    text-align: center;
    background: linear-gradient(to bottom, #ff6175, #d90030); /* gradient pink to dark red */
    color: white;
}

.hero h1 {
    font-size: 48px;
    font-weight: 900;
    line-height: 1.3;
}

.hero p {
    margin-top: 15px;
    font-size: 18px;
    opacity: 0.95;
}

/* =======================
   SEARCH CARD (White Square Box)
======================= */
.search-card {
    max-width: 1150px;
    background: #fff;
    margin: 40px auto 0 auto;
    padding: 25px 30px;
    border-radius: 20px; /* more square than before */
    display: flex;
    gap: 20px;
    align-items: center;
    justify-content: center;
    box-shadow: 0 15px 35px rgba(0,0,0,0.12);
}

.search-card input {
    width: 300px;
    padding: 16px 20px;
    font-size: 15px;
    border-radius: 12px; /* slightly rounded square */
    border: 1px solid #ddd;
    outline: none;
    background: #fff;
    transition: 0.3s;
}

.search-card input:hover {
    background: #f9f9f9;
    border-color: #ff3b4e;
}

.search-card button {
    padding: 16px 28px;
    background: #c70039; /* dark red */
    color: white;
    border: none;
    border-radius: 12px; /* square corners */
    font-size: 16px;
    font-weight: 600;
    cursor: pointer;
    transition: 0.3s;
}

.search-card button:hover {
    background: #900028;
}

/* =======================
   SECTION TITLES
======================= */
.section-title {
    margin-top: 70px;
    text-align: center;
    font-size: 38px;
    color: #d90030; /* dark red title */
    font-weight: 800;
}

/* =======================
   RESTAURANT CARDS
======================= */
.card-container {
    max-width: 1200px;
    margin: 50px auto;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 25px;
}

.restaurant-card {
    background: white;
    border-radius: 20px;
    overflow: hidden;
    box-shadow: 0 15px 35px rgba(0,0,0,0.1);
    transition: transform 0.3s, box-shadow 0.3s;
}

.restaurant-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 20px 45px rgba(0,0,0,0.15);
}

.restaurant-card img {
    width: 100%;
    height: 180px;
    object-fit: cover;
}

.restaurant-card .card-body {
    padding: 18px 20px;
}

.restaurant-card .card-body h3 {
    font-size: 20px;
    font-weight: 700;
    color: #d90030;
    margin-bottom: 6px;
}

.restaurant-card .card-body p {
    font-size: 14px;
    color: #555;
    margin-bottom: 10px;
}

.restaurant-card .card-body .price {
    font-weight: 600;
    color: #c70039;
}

/* =======================
   LOCATION BOXES
======================= */
.location-boxes {
    max-width: 1150px;
    margin: 50px auto;
    display: flex;
    gap: 20px;
    flex-wrap: wrap;
}

.location-box {
    flex: 1 1 200px;
    background: #fff;
    padding: 25px;
    border-radius: 16px; /* square-ish boxes */
    text-align: center;
    box-shadow: 0 12px 30px rgba(0,0,0,0.08);
    cursor: pointer;
    transition: 0.3s;
}

.location-box:hover {
    background: #ffe3e6;
    transform: translateY(-3px);
}

.location-box h4 {
    font-size: 18px;
    font-weight: 700;
    color: #ff3b4e;
}

/* =======================
   FOOTER
======================= */
footer {
    background: #ff3b4e;
    color: white;
    padding: 40px 20px;
    text-align: center;
    margin-top: 60px;
}

footer p {
    font-size: 16px;
}
