@charset "UTF-8";

body { font-family: Arial, sans-serif; background: #f8f9fa; margin:0; }
.container { width: 90%; margin:auto; }

/* HEADER */
header {
    background: linear-gradient(135deg, #ff3f4a 0%, #ff6b81 100%);
    padding:1rem 0; color:white; position:sticky; top:0; z-index:1000;
}
.navbar { display:flex; justify-content:space-between; align-items:center; flex-wrap:wrap; gap:1rem; }
.logo { display:flex; align-items:center; gap:0.5rem; }
.logo-text { font-weight:900; font-size:1.5rem; }
.nav-menu { display:flex; list-style:none; gap:1rem; }
.nav-menu a { color:white; font-weight:600; }
.nav-actions { display:flex; align-items:center; gap:1rem; }
.cart-icon-wrapper { position:relative; }
.cart-badge { position:absolute; top:-0.5rem; right:-0.5rem; background:#ff4444; color:#fff; border-radius:50%; width:1.25rem; height:1.25rem; display:flex; align-items:center; justify-content:center; font-size:0.75rem; font-weight:700; }

/* CART ITEMS */
.cart-container { margin:3rem 0; }
.back-link { display:inline-block; margin-bottom:1rem; color:#ff4757; }
.cart-item { display:flex; background:#fff; padding:15px; margin-bottom:12px; border-radius:10px; align-items:center; }
.cart-item img { width:100px; height:80px; border-radius:8px; margin-right:15px; object-fit:cover; }
.item-details { flex:1; }
.item-details h3 { margin:0; font-size:18px; }
.price { color:#ff4757; font-weight:bold; margin-top:8px; }
.qty-controls { display:flex; align-items:center; gap:8px; margin-top:6px; }
.qty-btn { padding:5px 10px; background:#ff4757; color:#fff; border:none; border-radius:5px; cursor:pointer; }
.qty-input { width:40px; text-align:center; padding:6px; border-radius:5px; border:1px solid #ccc; }
.remove-btn { background:transparent; border:none; color:red; font-size:18px; cursor:pointer; }

/* CART SUMMARY */
.cart-summary { background:#fff; padding:20px; border-radius:12px; margin-top:20px; box-shadow:0 5px 15px rgba(0,0,0,0.05); }
.summary-row { display:flex; justify-content:space-between; margin-bottom:12px; font-size:18px; }
.grand-total { font-weight:700; color:#ff4757; }
.checkout-btn { width:100%; padding:14px; background:#ff4757; border:none; color:#fff; border-radius:10px; margin-top:10px; font-size:18px; cursor:pointer; }
.clear-btn { width:100%; padding:12px; background:#333; color:#fff; margin-top:10px; border:none; border-radius:10px; cursor:pointer; }
.empty-cart { text-align:center; font-size:20px; margin-top:40px; color:#777; }

/* FOOTER */
footer { background:#2f3542; color:white; text-align:center; padding:2.5rem 0 1.5rem; margin-top:3rem; }

/* RESPONSIVE */
@media(max-width:768px){
    .cart-item { flex-direction:column; align-items:flex-start; }
    .qty-controls { margin-top:6px; }
}
