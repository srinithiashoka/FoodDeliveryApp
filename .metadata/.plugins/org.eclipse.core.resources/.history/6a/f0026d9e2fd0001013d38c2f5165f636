@charset "UTF-8";

/* =======================
   Menu Page Styles
======================= */
.menu-container {
    padding: 30px 0;
    max-width: 900px;
    margin: 0 auto;
}

.menu-item {
    display: flex;
    align-items: center;
    margin-bottom: 20px;
    padding: 12px;
    border-bottom: 1px solid #eee;
    position: relative;
    transition: all 0.3s ease;
}

.menu-item:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 20px rgba(0,0,0,0.1);
}

.menu-image {
    background: #fff;
    padding: 8px;
    border-radius: 12px;
    width: 120px;
    height: 120px;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-right: 20px;
    position: relative;
}

.menu-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 12px;
}

.sold-out-badge {
    position: absolute;
    top: 5px;
    left: 5px;
    background: #ff4757;
    color: #fff;
    padding: 4px 8px;
    border-radius: 5px;
    font-size: 0.8rem;
    font-weight: bold;
}

.menu-content {
    flex: 1;
}

.menu-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.menu-header h3 {
    margin: 0;
    font-size: 1.2rem;
    color: #ff4757;
}

.menu-description {
    margin: 5px 0 8px 0;
    color: #555;
}

.menu-price {
    font-weight: bold;
}

/* Quantity Controls */
.quantity-controls {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-top: 6px;
    flex-wrap: wrap;
}

.quantity-input-display {
    width: 40px;
    height: 36px;
    text-align: center;
    border-radius: 5px;
    border: 1px solid #ccc;
    background: #f7f7f7;
    font-size: 0.9rem;
}

.qty-btn {
    border: none;
    background: #ddd;
    padding: 6px 10px;
    border-radius: 5px;
    cursor: pointer;
    font-weight: bold;
    font-size: 0.85rem;
}

.qty-btn:hover { background: #bbb; }

.qty-btn.clear {
    background: #ff6b81;
    color: #fff;
}

.btn-add {
    background: #2ed573;
    color: white;
    border: none;
    padding: 0 16px;
    border-radius: 5px;
    cursor: pointer;
    font-weight: bold;
    font-size: 0.95rem;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background 0.3s;
}

.btn-add:hover { background: #26c35e; }

.qty-btn.clear, .btn-add { height: 36px; }

/* Back link & Title */
.back-link { display: inline-block; margin-bottom: 20px; font-size: 1rem; color: #0a3d62; text-decoration: none; }
.back-link:hover { text-decoration: underline; }
.menu-title { text-align: center; margin-bottom: 20px; color: #ff4757; }
.no-menu-items { text-align: center; color: red; }

/* Toast */
#toast {
    visibility: hidden;
    min-width: 150px;
    max-width: 220px;
    background-color: #2ed573;
    color: white;
    text-align: center;
    border-radius: 8px;
    padding: 12px;
    position: fixed;
    z-index: 9999;
    top: 20px;
    right: 20px;
    font-size: 14px;
    opacity: 0;
    transition: opacity 0.3s, visibility 0.3s;
    box-shadow: 0 2px 6px rgba(0,0,0,0.2);
}
#toast.show { visibility: visible; opacity: 1; }

/* Cart badge */
.cart-icon-wrapper { position: relative; display: inline-block; margin-left: 15px; }
.cart-icon-wrapper img { width: 34px; height: 34px; cursor: pointer; }
.cart-badge { position: absolute; top: -6px; right: -6px; background: #ff4757; color: white; font-size: 12px; font-weight: bold; padding: 2px 7px; border-radius: 50%; }

/* Responsive */
@media(max-width: 767px) {
    .menu-item { flex-direction: column; align-items: flex-start; }
    .menu-image { width: 100%; height: 200px; margin-bottom: 12px; }
    .quantity-controls { flex-wrap: wrap; gap: 6px; }
    .btn-add, .qty-btn.clear { flex: 1 1 100%; text-align: center; }
}
