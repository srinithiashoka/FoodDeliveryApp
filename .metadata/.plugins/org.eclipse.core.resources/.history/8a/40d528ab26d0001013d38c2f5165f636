/* =======================
   FOODHUB - FULL UPDATED CSS
   (Reduced Category Cards + Alignment Fixed + Black Footer)
======================= */

/* RESET - NO UNDERLINES */
* { margin: 0; padding: 0; box-sizing: border-box; }
a, a:hover, a:focus, a:active, a:visited { text-decoration: none !important; color: inherit !important; }

:root {
    --primary-gradient: linear-gradient(135deg, #ff6b81 0%, #ff4757 100%);
    --navbar-gradient: linear-gradient(135deg, #ff3f4a 0%, #ff6b81 100%);
    --success-gradient: linear-gradient(135deg, #2ed573, #26c35e);
    --secondary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    --card-height: 300px; /* REDUCED from 420px */
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
    color: #333;
    line-height: 1.6;
    overflow-x: hidden;
}

.container { width: 90%; max-width: 1200px; margin: 0 auto; }

/* NAVBAR */
header {
    background: var(--navbar-gradient);
    padding: 1rem 0;
    color: white;
    position: sticky;
    top: 0;
    z-index: 1000;
    box-shadow: 0 4px 20px rgba(255, 63, 74, 0.3);
}

.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 1rem;
}

.logo {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    cursor: pointer;
}

.logo-text {
    font-size: clamp(1.5rem, 5vw, 2.2rem);
    font-weight: 900;
    background: linear-gradient(45deg, #fff, #ffeaa7);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.scooter-icon {
    font-size: clamp(1.8rem, 8vw, 2.5rem);
    background: linear-gradient(45deg, #fff, #ffeaa7);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.nav-menu {
    display: flex;
    list-style: none;
    gap: 1.5rem;
    flex-wrap: wrap;
}

.nav-menu a {
    font-weight: 600;
    color: rgba(255,255,255,0.95) !important;
    padding: 0.75rem 1.5rem;
    border-radius: 25px;
    transition: 0.3s ease;
}

.nav-menu a:hover {
    color: #ffeaa7 !important;
    background: rgba(255,255,255,0.2);
    transform: translateY(-2px);
}

.nav-actions { display: flex; align-items: center; gap: 1rem; }

/* LOGIN/SIGNUP BUTTONS */
.auth-buttons { display: flex; gap: 0.75rem; align-items: center; }

.btn {
    padding: 0.75rem 1.5rem;
    border-radius: 2rem;
    cursor: pointer;
    font-weight: 600;
    font-size: 0.95rem;
    transition: 0.3s ease;
    min-width: 90px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
}

.btn-login {
    background: rgba(255,255,255,0.2);
    color: white !important;
    border: 1px solid rgba(255,255,255,0.3);
}

.btn-signup {
    background: var(--success-gradient);
    color: white !important;
}

.btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(0,0,0,0.3);
}

/* HERO SECTION */
.hero {
    background: var(--primary-gradient);
    color: white;
    text-align: center;
    padding: 8rem 1rem 6rem;
}

.hero-text h1 {
    font-size: clamp(2rem, 6vw, 4rem);
    margin-bottom: 1rem;
    font-weight: 800;
}

.hero-text p {
    font-size: clamp(1rem, 3vw, 1.3rem);
    opacity: 0.95;
    margin-bottom: 3rem;
}

/* SEARCH BOX */
.search-box {
    background: rgba(255,255,255,0.98);
    backdrop-filter: blur(20px);
    padding: 2rem;
    border-radius: 2.5rem;
    box-shadow: 0 25px 60px rgba(0,0,0,0.15);
    max-width: 1000px;
    margin: 0 auto;
}

.search-form {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    align-items: center;
}

/* CATEGORY SECTION */
.categories {
    padding: 5rem 0;
    background: rgba(255,255,255,0.85);
}

section h2 {
    text-align: center;
    margin-bottom: 4rem;
    font-size: clamp(2rem, 5vw, 3.2rem);
    color: #ff4757;
    font-weight: 800;
}

/* GRID */
.category-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr)); /* REDUCED size */
    grid-auto-rows: var(--card-height);
    gap: 2rem;
    max-width: 1100px;
    margin: 0 auto;
}

.category-card {
    background: white;
    border-radius: 2rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 1.5rem;
    box-shadow: 0 15px 35px rgba(0,0,0,0.12);
    transition: 0.25s ease;
}

.category-card:hover {
    transform: translateY(-10px);
}

.category-image {
    width: 70px;   /* reduced */
    height: 70px;  /* reduced */
    border-radius: 20px;
    overflow: hidden;
    margin-bottom: 1rem;
}

.category-card img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.category-card p {
    font-weight: 700;
    font-size: 1.05rem;
    margin: 0;
}

/* RESTAURANTS SECTION */
.restaurant-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    grid-auto-rows: var(--card-height);
    gap: 2rem;
}

.restaurant-card {
    background: white;
    border-radius: 2rem;
    overflow: hidden;
    box-shadow: 0 25px 50px rgba(0,0,0,0.15);
}

.restaurant-image { height: 200px; overflow: hidden; }

.restaurant-card img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: 0.3s ease;
}

.restaurant-card:hover img { transform: scale(1.08); }

.restaurant-info {
    padding: 1.5rem;
}

/* FOOTER — PURE BLACK */
footer {
    background: #000 !important;
    color: white;
    text-align: center;
    padding: 4rem 0 2.5rem;
    margin-top: 6rem;
}

footer p { font-size: 0.9rem; margin-top: 10px; }

/* RESPONSIVE */
@media (max-width: 480px) {
    .nav-menu { display: none; }
    .auth-buttons { flex-direction: column; gap: 0.5rem; }
}

img { loading: lazy; }
